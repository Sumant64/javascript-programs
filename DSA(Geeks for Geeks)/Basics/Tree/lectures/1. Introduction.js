// Introduction to tree

/**
 * o Non linear & hierarchical data structure.
 * o collection of nodes such that each node of the tree stores a value and a list of references to other nodes (the “children”).
 * 
 * Basic Terminology:
 * -----------------
 * o Parent Node:
 *      The node which is a predecessor of a node is called the parent node.
 * o Child Node:
 *      The node which is the immediate successor of a node is called child node.
 * o Root Node: 
 *      The topmost node of the tree.
 * o Leaf node or External Node:
 *      The nodes which do not have any child nodes are called leaf nodes.
 * o Ancestor of a Node:
 *      
 */